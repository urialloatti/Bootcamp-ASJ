import { PrimeNumbersFilterPipe } from './prime-numbers-filter.pipe';

describe('PrimeNumbersFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimeNumbersFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
